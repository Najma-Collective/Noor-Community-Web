{
  "name": "noor-community-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild.js",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d docs"
  },
  "dependencies": {
    "@material/material-color-utilities": "^0.3.0",
    "@material/web": "^2.4.1",
    "material-symbols": "^0.28.1"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "sass": "^1.79.4",
    "vite": "^5.4.6"
  }
}
